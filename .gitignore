# Ignore all derivated files created by issuing rpmbuild
# as we don't want to store that can and will be created
# and changed on every subsequent build.
BUILD/*
BUILDROOT/*
RPMS/*
SRPMS/*
tmp/*

# ignore sources that are big and therefore hosted in /gpfs/software/packages/*
SOURCES/NAMD_*
